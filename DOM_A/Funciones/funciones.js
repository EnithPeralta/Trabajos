/*1.
function sumaNumeros(num1, num2) {
    return num1 + num2;
}

const resultado = sumaNumeros(4, 6);
console.log("La suma es:", resultado); */

/*2.
function Arreglo(arr) {
    return arr.reduce((total, elemento) => total + elemento, 0);
}

const numeros = [2, 4, 6, 8];
const suma = Arreglo(numeros);
console.log("La suma de los elementos del arreglo es:", suma); */
/*3.
function esPalindromo(cadena) {
    const cadenaSinEspacios = cadena.toLowerCase().replace(/[^a-zA-Z0-9]/g, '');
    const cadenaReversa = cadenaSinEspacios.split('').reverse().join('');
    return cadenaSinEspacios === cadenaReversa;
}
// Ejemplo de uso:
const palabra = 'oso';
console.log(esPalindromo(palabra)); // Output: true

const texto = 'Anita lava la tina';
console.log(esPalindromo(texto)); // Output: true

const frase = 'Hola mundo';
console.log(esPalindromo(frase)); // Output: false
*/
